CASA EM JOGO — V1.1.6 INPUTFIX

CORRIGIDO:
ReferenceError: keys is not defined

A movimentação e os listeners consultavam `keys`, mas a variável
não existia mais depois das refatorações anteriores. Como update()
consulta isso em todo frame, o loop quebrava imediatamente.

Agora:
- `keys` existe antes dos listeners.
- existe exatamente uma declaração.
- keydown e keyup foram conferidos.
- perder foco/aba limpa teclas para evitar movimento preso.
- IA/pathfinding da V1.1.5 foi preservada.

QA:
- node --check: OK
- update(): único
- updateBots(): único
- loop(): único
- findPath(): único
- safePoint(): único
